using System.Drawing;

namespace XNBConverter
{
    public static class ThemeColors
    {
        // Main background (light olive)
        public static Color Background = Color.FromArgb(196, 199, 167);  // Light olive

        // Control backgrounds (slightly darker olive)
        public static Color ControlBackground = Color.FromArgb(176, 179, 147);  // Medium olive

        // Dark background for text boxes and list boxes (darker olive)
        public static Color DarkBackground = Color.FromArgb(156, 159, 127);  // Darker olive

        // Border color
        public static Color BorderColor = Color.FromArgb(116, 119, 87);  // Darkest olive

        // Text color
        public static Color TextColor = Color.FromArgb(51, 51, 51);  // Dark gray for better readability

        // Accent color for highlights
        public static Color AccentColor = Color.FromArgb(142, 140, 54);  // Deep olive
    }
}